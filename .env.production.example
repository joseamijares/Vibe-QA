# VibeQA Production Environment Variables
# Copy this file to .env.production and fill in your values

# === Supabase Configuration ===
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Service role key for server-side operations (Keep this secret!)
# Only needed for edge functions, not for the frontend
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# === Stripe Configuration ===
# Public key (safe to expose in frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key

# Secret keys for edge functions (Keep these secret!)
# Set these in Supabase edge function secrets:
# supabase secrets set STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
# supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
# supabase secrets set STRIPE_PRICE_BASIC=price_your-basic-plan-id
# supabase secrets set STRIPE_PRICE_FULL=price_your-full-plan-id

# === Email Configuration (Brevo) ===
# Set in Supabase edge function secrets:
# supabase secrets set BREVO_API_KEY=your-brevo-api-key

# === Application URLs ===
# Your production app URL (no trailing slash)
APP_URL=https://vibeqa.app

# === Optional: Error Monitoring ===
# Sentry DSN for error tracking
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# === Optional: Analytics ===
# Google Analytics or similar
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# === Build Configuration ===
NODE_ENV=production
VITE_MODE=production